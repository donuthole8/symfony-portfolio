{% block body %}
  <h1>{{ blog.title }}</h1>
  <p><small>{{ blog.createdAt|date('Y/m/d H:i') }}</small></p>
  <p>{{ blog.content|nl2br }}</p>
  <p><a href="{{ path('blog_index') }}">戻る</a></p>
{% endblock %}
